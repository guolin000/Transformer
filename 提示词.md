有什么推荐的小数据集适合我的硬件设备做这个模型的任务，并说明数据集用到的是 encoder还是decoder 还是encoder -decoder ： 
使用py（3.8），手工搭建一个Transformer,实现 multi-head self-attention、position-wise FFN、残差 +LayerNorm、位置编码等，有 encoder block、decoder block，并在小规模文本建模任务上训练并做消融实验。我的torch是2.4.1，设备是笔记本4060 8G+32G内存。 实验还有要求： 提交这个实验的 Git 仓库（GitHub link）包含：src/、requirements.txt、README.md、scripts/run.sh。 • results/ 放训练曲线图与表格。 • 在 README 写清楚运行命令与硬件要求，给出重现实验的 exact 命令行（含随机种子）
文件目录是
Transformer/ 
├── checkpoints/
├── src/
│   ├── __init__.py
│   ├── data/
│   │   ├── ted_zh_en_train.txt           # 小语料
│   │   ├── ted_zh_en_test.txt           # 小语料
│   │   ├── vocab.py                   # 构建词表
│   │   └── __init__.py
│   │
│   ├── models/
│   │   ├── __init__.py
│   │   ├── attention.py               # MultiHead Self-Attention
│   │   ├── feedforward.py             # Position-wise FFN
│   │   ├── positional_encoding.py     # 位置编码
│   │   ├── encoder_block.py           # Encoder Block
│   │   ├── decoder_block.py           # Decoder Block
│   │   ├── encoder.py                 # 堆叠 EncoderBlock
│   │   ├── decoder.py                 # 堆叠 DecoderBlock
│   │   └── transformer.py             # Transformer Encoder + Decoder
│   │
│   ├── utils/
│   │   ├── __init__.py
│   │   ├── config.py                  # 超参数
│   │   ├── dataset.py                 # 文本加载 + DataLoader
│   │   ├── mask.py                    # padding mask & subsequent mask
│   │   ├── train_utils.py             # 训练循环
│   │   ├── metrics.py                 # perplexity / BLEU
│   │   └── logger.py                  # 日志打印与训练可视化
│   │
│   ├── experiments/
│   │   ├── __init__.py
│   │   ├── ablation_no_posenc.py
│   │   ├── ablation_no_layernorm.py
│   │   ├── ablation_no_residual.py
│   │   ├── ablation_no_multihead.py
│   │   └── ablation_no_cross_attention.py
│   │
│   ├── main_train.py                  # 主训练脚本
│   └── evaluate.py                    # 模型评估与文本生成
│
├── scripts/
│   └── run.sh                          # 统一运行命令脚本（含随机种子）
│
├── results/
│   ├── loss_curves.png                 # 训练曲线
│   ├── metrics_table.csv               # 训练指标表格
│
├── requirements.txt                     # 环境依赖
└── README.md                            # 项目说明、硬件要求、运行命令 
